# Python source without hidden execution

`sim-codec-python` admits Python 3.14.6 text into a bounded, location-bearing,
lossless concrete tree. It preserves comments, layout, literal spellings,
f-strings, template strings, and soft keywords while keeping runtime behavior
outside the frontend.
