# Provider codec profiles

The provider profile operation returns the codec symbol, provider id, request
wire shape, and stream wire shape for the built-in OpenAI, Anthropic, Ollama,
LM Studio, and Lemonade projections. It performs no provider call and contains
no secret value.
