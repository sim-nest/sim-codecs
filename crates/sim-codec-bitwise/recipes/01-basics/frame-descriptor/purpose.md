# Frame descriptor

This recipe records the bitwise codec surface as data: self-delimiting frames,
shared tables, minimal-magnitude numbers, and the canonical byte string used for
content-addressing.
