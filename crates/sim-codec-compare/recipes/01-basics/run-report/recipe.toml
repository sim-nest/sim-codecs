id = "run-report"
title = "Run the comparison report"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["codec", "compare", "bitwise", "binary", "sandbox-descriptor"]
requires = ["core", "codec/lisp"]
