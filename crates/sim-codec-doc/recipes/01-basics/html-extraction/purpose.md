# Extract inert HTML

Project saved HTML into the shared `MarkupDoc`; active and embedded elements are omitted with fidelity warnings.

This is a sandbox descriptor because HTML extraction is exercised through the
typed document-codec boundary rather than a stable cookbook runtime function.
Crate tests execute extraction, omission, and fidelity-warning behavior.
