# Markup backend catalog

The document codec exposes a catalog of implemented and tracked markup
backends. Markdown, Typst, AsciiDoc, and LaTeX are installed as strict
read/write codecs; tracked entries such as Djot, Org, WikiText, and Texinfo
are cataloged without silently registering decoders.
