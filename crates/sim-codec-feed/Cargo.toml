[package]
name = "sim-codec-feed"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Bounded inert RSS, Atom, and JSON Feed codec for SIM."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-codecs"
homepage = "https://github.com/sim-nest/sim-codecs"

[dependencies]
serde_json = "1"
sim-cookbook = { version = "0.2.0" }

[build-dependencies]
sim-cookbook-build = { version = "0.1.0" }

[lints]
workspace = true
