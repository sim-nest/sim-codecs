# BRIDGE packet codec

The BRIDGE codec decodes one strict packet line form into a checked packet
expression and encodes that expression back to the same line form. This recipe
uses a small request packet with a frame and return part.
