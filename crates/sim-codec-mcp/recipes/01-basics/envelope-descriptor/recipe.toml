id = "envelope-descriptor"
title = "Envelope descriptor"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["codec", "mcp", "json-rpc", "sandbox-descriptor"]
requires = ["core", "codec/lisp"]
