id = "per-lib-config"
title = "Per-library config descriptor"
codec = "config"
setup = "setup.toml"
purpose = "purpose.md"
order = 10
tags = ["codec", "config", "table", "roundtrip"]
requires = ["codec/config"]
