id = "frame-descriptor"
title = "Frame descriptor"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["codec", "binary", "frame", "sandbox-descriptor"]
requires = ["core", "codec/lisp"]
