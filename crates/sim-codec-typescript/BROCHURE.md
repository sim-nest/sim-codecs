# TypeScript syntax without a compiler dependency

Parse TypeScript 7.0.2 and TSX into bounded, lossless syntax while reusing SIM's
JavaScript frontend for ECMAScript identity. Exact source, trivia, locations,
and extension context survive even where evaluation metadata does not yet exist.
